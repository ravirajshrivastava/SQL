-- DDL -> DRCAT --->>> DROP  RENAME  CREATE  ALTER  TRUNCATE



--LET SAY WORKING ON STAFF TABLE


-- CREATE A TABLE WITH CONSTRAINTS

CREATE TABLE STFAF
(
    STAFF_ID VARCHAR(20) --PRIMARY KEY,
    STAFF_TYPE VARCHAR(20),
    SCHOOL_ID VARCHAR(20),
    FIRST_NAME VARCHAR(100) NOT NULL,
    DOB DATE,
    AGE INT
    GENDER VARCHAR(10) CHECK GENDER(IN('MALE', 'M', 'FEMALE', 'F')),
    CONSTRAINT PK_STF PRIMARY KEY(STAFF_ID),
    ADDRESS_ID VARCHAR(20),
    CONSTRAINT FK_STAFF_SCHL FOREIGN KEY (SCHOOL_ID) REFERENCES SCHOOL(SCHOOL_ID),
    CONSTRAINT FK_STAFF_ADDR FOREIGN KEY (ADDRESS_ID) REFERENCES ADDRESS(ADDRESS_ID)
);



-- DELETE THE TABLE (IT REMOVES THE TABLE STRUCTURE AS WELL AS THE DATA IT CONSTAINS)
DROP TABLE STAFF;



-- RETRIEVE/Fetch AND VIEW THE TABLE
SELECT * FROM STAFF;



--DROP A COLUMN
ALTER TABLE STAFF
DROP COLUMN JOIN_DATE;



--CHANGE THE DATA TYPE OF A COLUMN
ALTER TABLE STAFF
ALTER COLUMN FIRST_NAME VARCHAR(100);



--RENAME A TABLE
ALTER TABLE STAFF 
RENAME TO STAFF_NEW;



--RENAME THE COLUMN
ALTER TABLE STAFF 
RENAME COLUMN FIRST_NAME TO FULL_NAME;



--ADD A CONSTRAINT TO A EXISTING TABLE
ALTER TABLE STAFF
ADD CONSTRAINT FK_STAFF_SCHL FOREIGN KEY(SCHOOL_ID) REFERENCES SCHOOL(SCHOOL_ID)

ANOTHER EXAMPLE     --Always name your constraints (e.g., UNQ_STF), as it makes managing constraints easier (e.g., if you need to drop or modify them later).

ALTER TABLE STAFF
ADD CONSTRAINT UNQ_STF UNIQUE (STAFF_TYPE);



--TRUNCATE TABLE
TRUNCATE TABLE STAFF;   --This command removes all rows from a table, but the table structure remains intact.




